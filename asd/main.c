#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    char term;
    if(scanf("%d%c", &num, &term) != 2 || term != '\n')
    printf("failure\n");
    else
    printf("valid integer followed by enter key\n");
    return 0;

    printf("\n\n");
    printf(" ")
}
