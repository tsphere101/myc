#include <stdio.h>
int main()
{
    int n, c, k;

    do
    {
        scanf("%d", &n);
    }
    while ()


        for (c = 1; c <= n; c++)
        {
            for(k = 1; k <= c; k++)
            {
                printf("printf(\"*\");");
            }

            printf("printf(\"\\n\");");
            printf("\n");
        }

    return 0;
}
